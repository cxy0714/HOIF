# Extract Diagonal as a Diagonal Matrix

Returns a diagonal matrix containing only the diagonal elements of the
input matrix.

## Usage

``` r
diag_Mat(mat)
```

## Arguments

- mat:

  A numeric square matrix.

## Value

A diagonal matrix formed from `diag(mat)`.
