# Hadamard (Element-wise) Matrix Product

Computes the element-wise (Hadamard) product of two matrices of the same
size.

## Usage

``` r
hadamard(mat1, mat2)
```

## Arguments

- mat1, mat2:

  Numeric matrices of identical dimensions.

## Value

A matrix containing the element-wise product.
